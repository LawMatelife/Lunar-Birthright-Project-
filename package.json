{
  "name": "lunar-birthright-v4-production",
  "version": "4.0.0",
  "private": true,
  "engines": { "node": "24.x" },
  "scripts": {
    "build:frontend": "node build.mjs && node privacy-patch.mjs && node conversion-seo-patch.mjs && node registry-cleanup-patch.mjs",
    "reconcile:atlas": "python scripts/reconcile_atlas.py",
    "build:ci": "npm run build:frontend",
    "build": "npm run build:frontend && npm run reconcile:atlas"
  }
}
